<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.ListMaker" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="android:colorBackground">@color/white</item>
        <item name="android:textColor">@color/black</item>
        <item name="android:fontFamily">sans-serif-light</item>
    </style>

    <style name="SearchBar">
        <item name="android:backgroundTint">@color/white</item>
    </style>

    <style name="navDrawer">
        <item name="android:backgroundTint">@color/white</item>
    </style>

    <style name="navHeader">
        <item name="android:backgroundTint">@color/white</item>
        <item name="android:textColor">@color/colorPrimary</item>
    </style>

    <style name="toolbar">
        <item name="android:background">@color/white</item>
        <item name="android:titleTextColor">@color/colorPrimary</item>
    </style>

    <style name="floatingBtn">
        <item name="android:backgroundTint">@color/colorSecondary</item>
    </style>

    <style name="button1">
        <item name="android:textColor">@color/white</item>
        <item name="android:backgroundTint">@color/colorPrimary</item>
    </style>

    <style name="button2">
        <item name="android:textColor">@color/white</item>
        <item name="android:backgroundTint">@color/green</item>
    </style>

    <style name="slider">
        <item name="haloColor">@color/colorSecondary</item>
        <item name="thumbColor">@color/colorPrimary</item>
        <item name="trackColor">@color/colorSecondary</item>
    </style>

    <style name="splash">
        <item name="android:background">@color/white</item>
    </style>

</resources>