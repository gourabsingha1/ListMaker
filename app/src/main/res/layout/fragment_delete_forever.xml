<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="20dp"
    tools:context=".fragment.DeleteForeverFragment">

    <TextView
        android:id="@+id/tvDeleteForever"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:text="Delete list forever?"
        android:textSize="20sp"/>

    <Button
        android:id="@+id/btnDeleteForeverOk"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/tvDeleteForever"
        android:layout_alignParentEnd="true"
        android:layout_marginTop="36dp"
        android:layout_marginEnd="20dp"
        android:text="OK" />

    <Button
        android:id="@+id/btnDeleteForeverCancel"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/tvDeleteForever"
        android:layout_marginTop="36dp"
        android:layout_marginStart="20dp"
        android:text="Cancel" />

</RelativeLayout>